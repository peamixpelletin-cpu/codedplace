# Codedplace

Mobile-first slide-driven hide & seek controller.

## Requirements
- Node.js 18+

## Setup
```bash
npm install
npm start
```

Open `http://localhost:3000` on phones or desktops on the same network.

## Notes
- Login IDs: 732800 (Admin), 819223 (User), 019765 (User).
- Audio placeholders live in `public/sounds/`.
- State is in-memory only.
